import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  flatlist: {
    paddingVertical: 30
  },
  item: {
    padding: 10,
    borderBottomColor: "gray",
    borderBottomWidth: 1
  }
});
